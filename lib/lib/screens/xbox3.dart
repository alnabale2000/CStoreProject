import 'package:flutter/material.dart';

class XBox3Store extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('XBox3Store STORE'),
        ),
      ),
    );
  }
}
