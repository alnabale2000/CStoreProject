import 'package:flutter/material.dart';

class XBox5Store extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('XBox5Store STORE'),
        ),
      ),
    );
  }
}
