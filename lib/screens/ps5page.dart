import 'package:flutter/material.dart';

class Ps5Store extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Container(
          child: Text('PS5 STORE'),
        ),
      ),
    );
  }
}
